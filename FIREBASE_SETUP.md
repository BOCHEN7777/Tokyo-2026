# 东京攻略多人实时同步设置

网站已经内置 Firebase Realtime Database 实时同步。完成以下一次性设置后，两台手机可共享待办、记账、购物愿望单、预约和行李文字状态。

## 1. 创建 Firebase 项目

1. 打开 [Firebase Console](https://console.firebase.google.com/)，选择“创建项目”。
2. 项目名称可填写 `Tokyo-2026`；Google Analytics 可不启用。
3. 在项目首页点击 Web 图标 `</>`，注册一个 Web 应用。
4. 复制页面给出的完整 `firebaseConfig` 对象。

## 2. 启用匿名登录

1. 进入 **Authentication → Sign-in method**。
2. 启用 **Anonymous / 匿名** 登录方式。

## 3. 创建 Realtime Database

1. 进入 **Realtime Database → Create Database**。
2. 选择可用的亚洲区域。
3. 先选择锁定模式；不要将数据库长期设为公开读写。
4. 打开 **Rules**，用仓库根目录的 [`firebase-rules.json`](firebase-rules.json) 完整替换现有规则并发布。

## 4. 在攻略网站创建共享房间

1. 打开攻略网站的“多人实时同步”。
2. 粘贴完整 `firebaseConfig`。
3. 填写自己的名字，例如“没头脑”。
4. 点击“生成安全房间码”。
5. 点击“连接并开始同步”。首次连接会把本机现有核心数据上传到空房间。
6. 点击“复制邀请链接”，只发送给同行人。
7. 同行人打开链接，填写自己的名字，例如“不高兴”，再连接即可。

## 同步范围

- 实时同步：固定与自定义待办、预算和消费记录、购物愿望单、预约文字信息、行李状态、航班补充信息与旅行备忘。
- 仅本机保存：预约二维码、行李照片、汇率缓存、今日预览状态和 Firebase 房间配置。
- 断网时：修改先保存在浏览器；恢复网络且网页仍打开时自动上传。长期离线或清理浏览器前，仍建议导出完整备份。

## 安全提示

- Firebase Web 配置本身不是管理员密钥，但共享房间码相当于本次旅行的访问凭证。
- 邀请链接只发给同行人，不要发布到公开页面或社交平台。
- 不要在网站中保存护照照片、银行卡、安全码或其他高敏感信息。
- 若邀请链接泄露，退出共享后生成新的房间码，并在新房间重新同步。

